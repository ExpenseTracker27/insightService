[project]
name = "insightservice"
version = "0.1.0"
description = "Insight Service for expense tracking from messages"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    {name = "ExpenseTracker Team"}
]
dependencies = [
    "fastapi[all]>=0.115.12",
    "kafka-python>=2.2.3",
    "langchain-core>=0.3.10",
    "langchain-openai>=0.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
